import { motion } from "framer-motion"; import styled from "@emotion/styled"; interface FeedingMachineProps { onFeed: () => void; isFeeding: boolean; disabled: boolean; } const MachineContainer = styled.div`position: absolute; top: 20%; left: 50%; transform: translateX(-50%); width: 120px; height: 150px; background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%); border-radius: 15px; box-shadow: 0 10px 20px rgba(0,0,0,0.1); display: flex; flex-direction: column; align-items: center; justify-content: space-between; padding: 15px; z-index: 2;`; const FeedButton = styled(motion.button)<{ disabled: boolean }>`width: 60px; height: 60px; border-radius: 50%; background-color: ${props => props.disabled ? "#ccc" : "#e74c3c"}; color: white; border: none; font-size: 1rem; font-weight: bold; cursor: ${props => props.disabled ? "not-allowed" : "pointer"}; box-shadow: 0 4px 8px rgba(0,0,0,0.2); margin-top: 10px;`; const MachineName = styled.div`font-size: 0.9rem; color: #2c3e50; margin-bottom: 10px; background: rgba(255,255,255,0.8); padding: 5px 10px; border-radius: 10px;`; const FeedingMachine = ({ onFeed, isFeeding, disabled }: FeedingMachineProps) => { return ( <MachineContainer><MachineName>投食机</MachineName><FeedButton disabled={disabled} onClick={onFeed} whileHover={!disabled ? { scale: 1.1 } : {}} whileTap={!disabled ? { scale: 0.95 } : {}} animate={isFeeding ? { y: [0, -10, 0], scale: [1, 1.1, 1] } : {}} transition={{ duration: 0.5 }}>投食</FeedButton></MachineContainer> ); }; export default FeedingMachine;
